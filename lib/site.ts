import { env } from "@/lib/env";

export const siteConfig = {
  name: "Cloud Drive",
  description:
    "A secure internal file workspace for uploads, controlled sharing, recovery flows, and audit visibility.",
  url: env.appBaseUrl,
};
